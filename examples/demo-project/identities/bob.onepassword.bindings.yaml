# Simulated developer Bob — 1Password bindings (fake-op CI path).
version: "0.1"

capabilities:
  github.user.read:
    provider: onepassword
    onepassword:
      refs:
        GITHUB_TOKEN: "op://pade-demo/developers/bob/github/credential"
