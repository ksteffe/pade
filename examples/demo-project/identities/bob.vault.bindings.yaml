# Simulated developer Bob — Vault -dev bindings (prototype only).
version: "0.1"

capabilities:
  github.user.read:
    provider: vault
    vault:
      path: secret/pade/developers/bob/github
      fields:
        token: GITHUB_TOKEN
