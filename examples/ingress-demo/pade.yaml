# Narrative-only capability declaration for the Teleport ingress dogfood.
# No secrets. Teleport owns authenticated ingress; PADE does not.
apiVersion: pade.local/v1alpha1
kind: DevelopmentSession
metadata:
  name: ingress-demo
spec:
  capabilities:
    ingress.demo.read:
      access: read
