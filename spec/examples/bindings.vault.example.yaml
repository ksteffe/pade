# Example Vault -dev capability bindings (prototype only; not production-safe).
# Copy pattern into .pade/bindings.yaml or pass --bindings. Do not commit real credentials.
version: "0.1"

capabilities:
  github.user.read:
    provider: vault
    vault:
      path: secret/pade/github
      fields:
        token: GITHUB_TOKEN
